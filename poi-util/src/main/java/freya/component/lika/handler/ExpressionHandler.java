package freya.component.lika.handler;

import freya.component.lika.pojo.Expression;

public class ExpressionHandler {
//    public ExpressionHandler parseExpression(String expressionStr) {
//        Expression expression = new Expression();
//        if (expression == null || !expressionStr.trim().startsWith("$")) {
//            return null;
//        }
//        // 解析名称
//        String name = expressionStr.substring(expressionStr.indexOf('$'), expressionStr.indexOf('{')).trim();
//        if (name == null || name.isEmpty()) {
//            return null;
//        }
//        expression.setName(name);
//        // 解析handler
//
//    }
}
